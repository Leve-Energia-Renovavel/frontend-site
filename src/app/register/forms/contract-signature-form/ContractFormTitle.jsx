import { Typography } from "@mui/material";

export default function ContractFormTitle() {
    return (
        <div>
            <Typography variant="body1" component="h1">São apenas 3 documentos</Typography>
            <Typography variant="body1" component="h1">para assinar</Typography>
        </div>

    );
}
