import { Typography } from "@mui/material";

export default function SuccessFormTitle() {
    return (
        <div>
            <Typography variant="body1" component="h1">Assinaturas feitas</Typography>
            <Typography variant="body1" component="h1">com sucesso!</Typography>
        </div>

    );
}
