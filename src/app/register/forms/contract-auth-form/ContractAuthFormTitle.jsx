import { Typography } from "@mui/material";

export default function ContractAuthFormTitle() {
    return (
        <div>
            <Typography variant="body1" component="h1">Agora só falta confirmar</Typography>
            <Typography variant="body1" component="h1">o Token de Autenticação</Typography>
        </div>

    );
}
